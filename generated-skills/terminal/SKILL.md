---
name: terminal
description: Auto-generated by Mycelium from a recurring agent workflow (confidence 0.90). Detected sequence: terminal -> terminal.
---

# Recurring workflow (5x): terminal -> terminal

## Origin
Discovered by the Mycelium substrate — evidence: n=5 sessions share the sequence [terminal -> terminal]; agents=3; extracting as a compound skill would collapse 10 calls into 1

## Steps
1. Invoke `terminal` (the discovered next step in this workflow).
2. Invoke `terminal` (the discovered next step in this workflow).

## When to use
Trigger when the same multi-step sequence starts recurring. This skill collapses
the raw calls into one compound invocation.

## Pitfalls
- Re-verify each step's output before proceeding; the pattern was learned from
  successful past runs but the environment may have changed.
