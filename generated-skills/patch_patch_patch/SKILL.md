---
name: patch_patch_patch
description: Auto-generated by Mycelium from a recurring agent workflow (confidence 0.97). Detected sequence: patch -> patch -> patch.
---

# Automation opportunity: 41 calls saved via 'patch_patch_patch'

## Origin
Discovered by the Mycelium substrate — evidence: sequence [patch -> patch -> patch] ran 14x; a compound tool 'patch_patch_patch' replaces 42 calls with 1 (net 41 saved)

## Steps
1. Invoke `patch` (the discovered next step in this workflow).
2. Invoke `patch` (the discovered next step in this workflow).
3. Invoke `patch` (the discovered next step in this workflow).

## When to use
Trigger when the same multi-step sequence starts recurring. This skill collapses
the raw calls into one compound invocation.

## Pitfalls
- Re-verify each step's output before proceeding; the pattern was learned from
  successful past runs but the environment may have changed.
